# ==================== 商业化系统配置 ====================

# 数据库配置（与 commercial 共享）
DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/idoctor_commercial

# JWT配置
JWT_SECRET_KEY=your-super-secret-key-at-least-32-characters-long
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# 认证和配额开关
ENABLE_AUTH=true   # 开发时可设为 false
ENABLE_QUOTA=true  # 开发时可设为 false

# CORS配置（添加前端地址）
CORS_ORIGINS=http://localhost:3000,http://localhost:7500,http://localhost:4200

# ==================== SAM2 分割服务配置 ====================
# SAM2 服务地址（开发环境使用 localhost，生产环境使用 Docker 服务名 sam2_service）
SAM2_SERVICE_URL=http://localhost:8000
# SAM2_SERVICE_URL=http://sam2_service:8000  # Docker 环境

# 是否启用 SAM2 服务（true/false）
SAM2_ENABLED=true

# SAM2 请求超时时间（秒）
SAM2_REQUEST_TIMEOUT=30

# SAM2 缓存过期时间（小时）
SAM2_CACHE_EXPIRE_HOURS=24

# ==================== 原有配置保持不变 ====================
