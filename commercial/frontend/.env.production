# Nginx 网关地址（生产环境 - 使用相对路径，自动使用当前域名）
# 设置为空字符串，这样 API 请求会使用相对路径如 /api/xxx
# 例如：用户访问 http://ai.bygpu.com:55305，API 会请求 http://ai.bygpu.com:55305/api/xxx
VITE_NGINX_BASE_URL=

# API 服务端点（生产环境 - 使用Nginx反向代理）
# 注意：所有API统一使用/api/前缀，具体服务路径在代码中指定
VITE_AUTH_API_URL=/api
VITE_PAYMENT_API_URL=/api
VITE_IDOCTOR_API_URL=/api
# Flask前端应用URL（使用Nginx代理的相对路径）
VITE_IDOCTOR_APP_URL=/ctai

# 应用配置
VITE_APP_NAME=iDoctor 专业医疗影像分析平台
VITE_APP_VERSION=1.0.0

# 生产配置
VITE_DEV_MODE=false
VITE_MOCK_PAYMENT=false